//  @ts-check

/** @type {import('prettier').Config} */
const config = {
    semi: true,
    tabWidth: 4,
    singleQuote: false,
    trailingComma: "all",
    bracketSpacing: false,
    arrowParens: "avoid",
    // singleAttributePerLine: true,
};

export default config;
