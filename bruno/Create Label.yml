info:
  name: Create Label
  type: http
  seq: 3

http:
  method: POST
  url: https://localhost:5000/api/v1/label/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
